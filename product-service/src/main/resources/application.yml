spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mariadb://mariadb:3306/application
    username: admin
    password: admin

  flyway:
    url: jdbc:mariadb://mariadb:3306/application?currentSchema=product
    user: admin
    password: admin

server:
  port: 8002

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8001/eureka